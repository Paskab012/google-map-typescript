import React, {useState} from 'react';
import Map from './Map';

function App() {
  const [scriptLoaded, setScriptLoaded] = useState(false);
  
  
  return (
    <div className="App">
      Hello
    </div>
  );
}

export default App;
